## Summary

No new specifications required.

This change is a pure refactoring task (class renaming) that does not introduce new capabilities or modify existing requirements. The database schema remains unchanged, and all API contracts stay the same.